# Base URL the SPA uses to reach the API.
# Default "/api/v1" works behind the nginx /api proxy (docker compose) and the
# Vite dev proxy. Set an absolute URL to target a remote API directly.
VITE_API_URL=/api/v1
